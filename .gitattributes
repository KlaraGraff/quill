# Preserve the upstream formatting of the vendored reader engine.
public/foliate-js/** -whitespace

# PDF xref records contain significant fixed-width trailing spaces.
tests/fixtures/reader-compat/*.pdf binary
